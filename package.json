{
  "name": "nostr-ws-inspector",
  "version": "1.1.0",
  "description": "Browser extension to inspect Nostr WebSocket traffic",
  "scripts": {
    "build": "tsc && cp src/devtools.html dist/ && cp src/panel.html dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.3"
  }
}

