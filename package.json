{
  "name": "nostr-ws-inspector",
  "version": "1.1.0",
  "description": "Browser extension to inspect Nostr WebSocket traffic",
  "scripts": {
    "build": "tsc && cp src/devtools.html . && cp src/panel.html . && cp src/kinds.js .",
    "watch": "tsc --watch",
    "clean": "rm -f *.js devtools.html panel.html"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.3"
  }
}

