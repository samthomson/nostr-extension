{
  "name": "nostr-ws-inspector",
  "version": "1.10.0",
  "description": "Browser extension to inspect Nostr WebSocket traffic",
  "scripts": {
    "build": "tsc && cp src/devtools.html dist/ && cp src/panel.html dist/ && cp manifest.json dist/ && cp privacy-policy.md dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist/",
    "package": "npm run build && cd dist && zip -r ../extension.zip . -x '*.DS_Store'"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.3.3"
  }
}

